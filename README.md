# Metagenomics network hub website source

Built using mkdocs and mike

## Create the environment for build

`conda create --name myenv --file conda_env.txt`

## Deploy to github pages

Build and deploy website using:

`mkdocs gh-deploy`